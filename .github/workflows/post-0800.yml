name: "Threads Post 08:00 JST"

on:
  schedule:
    - cron: "0 23 * * *"
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: threads-post-0800
  cancel-in-progress: false

env:
  # ドライラン期間中は "true"。切り替え完了時にこの行を "false" に変更してpushする。
  DRY_RUN: "false"

jobs:
  post:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Post queue/0800.txt
        env:
          THREADS_TOKEN: ${{ secrets.THREADS_TOKEN }}
        run: bash scripts/post.sh queue/0800.txt

      - name: Post queue2/0800.txt
        env:
          THREADS_TOKEN: ${{ secrets.THREADS_TOKEN }}
        run: bash scripts/post.sh queue2/0800.txt
