name: "Threads Post 20:00 JST"

on:
  schedule:
    - cron: "0 11 * * *"
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: threads-post-2000
  cancel-in-progress: false

env:
  # ドライラン期間中は "true"。切り替え完了時にこの行を "false" に変更してpushする。
  DRY_RUN: "false"

jobs:
  post:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Post queue/2000.txt
        env:
          THREADS_TOKEN: ${{ secrets.THREADS_TOKEN }}
        run: bash scripts/post.sh queue/2000.txt

      - name: Post queue2/2000.txt
        env:
          THREADS_TOKEN: ${{ secrets.THREADS_TOKEN }}
        run: bash scripts/post.sh queue2/2000.txt
