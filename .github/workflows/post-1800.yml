name: "Threads Post 18:00 JST"

on:
  schedule:
    - cron: "0 9 * * *"
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: threads-post-1800
  cancel-in-progress: false

env:
  # ドライラン期間中は "true"。切り替え完了時にこの行を "false" に変更してpushする。
  DRY_RUN: "false"

jobs:
  post:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Post queue/1800.txt
        env:
          THREADS_TOKEN: ${{ secrets.THREADS_TOKEN }}
        run: bash scripts/post.sh queue/1800.txt

      - name: Post queue2/1800.txt
        env:
          THREADS_TOKEN: ${{ secrets.THREADS_TOKEN }}
        run: bash scripts/post.sh queue2/1800.txt
